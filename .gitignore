# Raw Kaggle data — too large for git; users supply locally.
# See README.md §6 for sourcing.
market_train_full.csv
news_train_*.csv

# Pipeline-generated artifacts (regenerated by the scripts).
# Keep them out of git; the small JSON reports are committed instead.
*.parquet
pipeline/*.log

# Python noise
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/

# OS noise
.DS_Store
Thumbs.db
